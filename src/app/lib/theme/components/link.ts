import { SystemStyleObject } from '@chakra-ui/theme-tools'

const baseStyle: SystemStyleObject = {
  _active: {
    boxShadow: 'none',
  },
}

const defaultProps = {}

export default {
  baseStyle,
  defaultProps,
}
